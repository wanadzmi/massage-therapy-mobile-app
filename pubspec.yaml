name: therapy_massage_app
description: "Professional therapy and massage booking application."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # C
  cached_network_image: ^3.3.1
  cupertino_icons: ^1.0.8
  
  # D
  dio: ^5.9.0
  
  # F
  firebase_core: ^4.2.0
  flutter_carousel_widget: ^2.3.0
  
  # G
  geolocator: ^13.0.2
  get: ^4.7.2
  
  # I
  image_picker: ^1.1.2
  intl: any
  intl_phone_field: ^3.2.0
  
  # S
  shared_preferences: ^2.5.3
  socket_io_client: ^2.0.3+1
  
  # T
  table_calendar: ^3.1.2
  
  # U
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
